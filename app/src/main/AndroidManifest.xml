<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.ghapshapkaro">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/videocallpic_foreground"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/videocallpic_foreground"
        android:supportsRtl="true"
        android:theme="@style/Theme.GhapShapKaro">
        <activity android:name=".DashboardActivity"></activity>
        <activity
            android:name=".SignupActivity"
            android:label="New Account" />
        <activity
            android:name=".LoginActivity"
            android:label="Login" />
        <activity
            android:name=".MainActivity"
            android:theme="@style/Theme.NoActionBar">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>